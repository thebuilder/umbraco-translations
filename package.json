{
  "name": "thebuilder-translations-workspace",
  "private": true,
  "packageManager": "pnpm@11.13.0",
  "scripts": {
    "build": "pnpm --dir src/TheBuilder.Translations/Client build",
    "typecheck": "pnpm --dir src/TheBuilder.Translations/Client typecheck",
    "lint": "pnpm --dir src/TheBuilder.Translations/Client lint",
    "fix": "pnpm --dir src/TheBuilder.Translations/Client fix",
    "deadcode": "fallow dead-code",
    "check": "pnpm typecheck && pnpm lint && pnpm deadcode",
    "test": "pnpm --dir src/TheBuilder.Translations/Client test",
    "audit-changes": "fallow audit --base main"
  },
  "devDependencies": {
    "fallow": "^3.17.0"
  }
}
